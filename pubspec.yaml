name: flutter_admob_view
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  google_mobile_ads: ^5.0.0
  flutter:
    sdk: flutter
dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
